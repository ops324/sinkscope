# SinkScope 技術仕様書

README が「何を・なぜ作るか」のピッチだとすれば、本書は「実際に何を、どう検証して
作ったか」の技術詳細である。特に第4章（データソースの実アクセス方法）は、公開情報だけ
からは分からず実機検証して初めて分かった内容を記録している。

---

## 1. スコープと構成

SinkScope は1プロダクト・2モジュール構成を取る。

- **Monitor**：100%実データ。国土地理院の衛星SAR変位速度を核に、地盤・地形・道路・
  過去の陥没事案という実データを同一の空間グリッドに統合する。
- **Triage**：Monitor の実成果を点検優先度に翻訳する意思決定支援層。疑似管路データを
  ここに限定し、`Illustrative` バッジで実データと区別する（設計方針は README 参照）。

本書は主に Monitor のデータ基盤（実装済み）を扱う。Triage の詳細設計は第9章、
フロントエンドUI（Monitor コックピット・初見向け配慮）の設計判断は第10章を参照。

---

## 2. データモデル

すべて `backend/core/models.py` に定義。共通の空間単位は `MeshCell`（250mメッシュ）で、
他の全テーブルはこれに `ForeignKey`/`OneToOneField` で紐づく。

| モデル | 主なフィールド | 内容 | 実データ元 |
|---|---|---|---|
| `MeshCell` | `mesh_code`, `geom`, `centroid` | 全レイヤー共通の250m格子 | ― |
| `DisplacementVelocity` | `mesh_cell`, `fiscal_year`, `velocity_cm_per_year`, `acquisition`(FK) | 準上下方向の変位速度（cm/年） | 国土地理院 衛星SAR地盤変動測量成果 |
| `DisplacementAcquisition` | `content_sha256`(unique), `retrieved_at`, `fiscal_year`, `fiscal_year_provenance` | 変位速度ラスタ取得ごとの監査記録（来歴の正、4.1章参照） | ― |
| `GroundClass` | `mesh_cell`, `land_use_class`, `elevation_m`, `geology_class`*, `ground_classification`* | 土地利用・標高（geology/ground_classificationは未実装、下記参照） | KSJ土地利用細分メッシュ、地理院標高タイル |
| `RoadExposure` | `mesh_cell`, `road_length_m` | メッシュ内道路延長（曝露オフセット用） | OpenStreetMap |
| `SubsidenceEvent` | `mesh_cell`, `fiscal_year`, `location_text`, `cause_facility`, `geocode_confidence` | 道路陥没事案（ジオコーディング済み） | 国土交通省 道路陥没オープンデータ |
| `PrecipitationObservation` | (未使用) | 降水量 | 実装見送り（第6章） |

**`DisplacementVelocity` がなぜ「日付ごとの累積変位」ではなく「年度ごとの速度」なのか**：
当初は稠密な時系列（複数時点の累積変位）を格納する設計だったが、実際に国土地理院のAPIを
呼んで確認した結果、一括ダウンロードで得られるのは年度時点での「変位速度」（観測期間全体の
線形トレンド、cm/年）1枚であり、多時点の時系列ではないと判明した。モデルはこの実態に
合わせて設計し直してある（`fiscal_year` を主キーの一部にすることで、将来別年度の成果を
同じテーブルに追加し年度間比較ができるようにしている）。詳細は第7章。

`GroundClass.geology_class` / `ground_classification` は将来の拡張用フィールドとして
モデルには残っているが、対応する取込スクリプトは未実装（KSJの「地質」データセットは
土地利用細分メッシュとは別データセットで、防災科学技術研究所のJ-SHIS(AVS30)等の代替も
検討したが今回は見送った）。

---

## 3. 空間設計

- **保存用座標系**：JGD2011 地理座標系（EPSG:6668）。全テーブルの `geom`/`centroid` はここに統一。
- **面積・距離計算用の投影座標系**：JGD2011 / 平面直角座標系 IX系（EPSG:6677、関東域）。
  `core/mesh.py` の `PROJECTED_SRID`。対象エリアが関東地方以外に広がる場合はここを見直す。
- **メッシュ**：250m四方の等面積グリッド。JIS X0410 の標準地域メッシュ（4分の1地域メッシュ）
  ではなく、独自の投影座標ベースグリッドを採用した。理由：等面積性が保証され、境界の
  丸め誤差がJIS標準メッシュの多段階換算より単純だったため。`mesh_code` は `"250m-{ix}-{iy}"`
  という格子インデックスで、公的な地域メッシュコードとは対応しない。
- **対象エリア**：`core/aoi.py` の `DEMO_AOI_BBOX`。江東区・江戸川区（東京都）・八潮市
  （埼玉県）周辺、(west, south, east, north) = (139.75, 35.62, 139.95, 35.86)。
  対象範囲で250mメッシュを生成すると7,918セルになる（実行・確認済み）。

---

## 4. データソースと検証済みアクセス方法

5つのデータソースすべて、実際にAPI/ダウンロードを叩いて中身を確認した。公開ドキュメント
だけでは分からなかった実装上の要点を記録する。

### 4.1 国土地理院 衛星SAR地盤変動測量成果（`ingest/gsi_displacement.py`）

`sarprod.gsi.go.jp` の地図UIで「データ範囲選択」→「経度緯度指定」→「データダウンロード」
と操作した際に実際に発行されるHTTPリクエストをブラウザのfetch/XHRをフックして捕捉した
結果、**ブラウザセッションに一切依存しない2段階のJSON APIであることを確認した**：

```
POST https://sarprod.gsi.go.jp/execCommand/
  {"type": ["3"], "coordinates": [west, east, south, north]}
  → {"file_name": "...", "file_size_MB": ..., "zip_path": "...", "cmd_types": ["3"]}

POST https://sarprod.gsi.go.jp/downloadfile/
  {"file_info": <execCommandのレスポンスそのまま>}
  → zip バイナリ（中に GeoTIFF 1枚）
```

`type: "3"` が「準上下方向」変位速度（南行=1、北行=2、準東西=4 とレイヤー定義ファイル
`layers_for_sar.txt` から対応付け）。Cookie無しの `requests` セッションで完全に再現できる
ことを確認済み（`docker compose exec backend python -c "..."` で実データを取得しテスト）。

取得したGeoTIFFは EPSG:6668、float32、空間分解能約90m。対象エリアの実測値は
**-1.80〜+0.10 cm/年、平均-0.072 cm/年**（沈降側に偏る）で、江東区・江戸川区が
地盤沈下による「ゼロメートル地帯」として知られていることと整合する。

**重要な制約**：このAPIで得られるのは常に「現在の年度成果」1枚の変位速度スナップショット
であり、稠密な多時点の時系列ではない。マップUIの「時系列グラフ閲覧」機能は点単位の閲覧を
想定しており、エリア一括の時系列ダウンロードに対応するAPIは（レイヤー設定ファイルを見る
限り）現時点で見当たらない。「2023年度成果」フォルダも別手法（緊急解析）のプロダクトで、
単純な年度間比較には使えない。この制約が第7章の未決定事項に直結する。

**来歴（provenance）の扱い**：上記の制約により、このAPIには年度を選択するパラメータが
無く、取込側が「これは何年度の成果か」を断定できる根拠は存在しない。当初の実装は
`ingest/gsi_displacement.py`に`FISCAL_YEAR = "2025"`という固定リテラルを持ち、取得した
ラスタの実ヴィンテージに関係なく全レコードへ無条件でこの年度を刻んでいた。これは
後日（例えば2027年）に再取込した場合、別ヴィンテージのラスタを"2025"と誤ラベルする
来歴汚染バグであり、敵対的監査で指摘・修正された（本節はその修正後の設計を記す）。

修正後の設計：
- 取得したGeoTIFFバイト列のSHA-256（`DisplacementAcquisition.content_sha256`、一意制約
  あり）を**真のヴィンテージ識別子**として毎回記録する。`fiscal_year`はあくまで
  best-effortの表示用ラベルに過ぎない。
- 年度ラベルは`fiscal_year_provenance`で信頼度を必ず併記する：`file_name`/`zip_path`
  （execCommand応答）またはZIP内エントリ名から一意に導出できた場合は`"filename"`、
  呼び出し側が明示指定した場合は`"operator_override"`、いずれも不能な場合は取得時点の
  日本の年度（JSTで4月始まり）を推定した`"unverified_retrieval_time"`。
  GSIの成果公開には数か月単位のラグがある（例：2025年度成果は2026年3月31日公開）ため、
  「取得時計の年度」が実際に配信されている成果の年度と一致する保証はない。
- **実際のfile_name/zip_pathの中身は本書執筆時点でまだ一度も実機観測されていない**
  （下記コマンド例のレスポンスは構造のみで値は伏せている）。そのため`file_name`等からの
  年度導出ロジックは実装を凍結しており（`ingest/gsi_displacement.py::_derive_fiscal_year`
  は常に`None`を返す）、現状は`"unverified_retrieval_time"`が事実上の既定経路になる。
  実フォーマットを確認するまでは、誤った確信を来歴に刻むより「未検証」と正直に示す方を
  選んでいる（誠実性フレームワーク、第5章）。
- 同一ラスタ（同一`content_sha256`）の再取得は`DisplacementAcquisition`を重複作成せず、
  既存の年度ラベルを再利用する。デデュープしないと、公開ラグの下で同一ラスタを繰り返し
  取り込むたびに異なる推定年度が刻まれ、`latest_fiscal_year()`の判定を汚染しかねない。
- `latest_fiscal_year()`（`analysis/features.py`）は年度文字列の大小では選ばない。
  検証済み（`"filename"`/`"operator_override"`）のラベルを優先し、無ければ
  `retrieved_at`が最新の取得記録を採用する。文字列の大小で選ぶと、未検証の推定年度が
  「数字が大きいから」という理由だけで検証済みの年度を追い越してしまう。

### 4.2 国土数値情報（KSJ）土地利用細分メッシュ（`ingest/kokudo_suuchi.py`）

- 配布は**1次メッシュ（約80km四方）単位**。ファイル名は
  `L03-b-{年度2桁}_{1次メッシュ4桁}-{測地系}_GML.zip`。対象エリアは1次メッシュ
  **`5339`** 1枚に収まる（実際にdatalistページのHTMLを取得し `DownLd()` 呼び出しの
  パスパターンから確認）。
- zipには Shapefile（属性はShift-JIS）と GeoJSON（UTF-8、`crs`フィールドに
  `EPSG::6668` と明記）が同梱。**文字コード問題を避けるためGeoJSONを読む**。
- 1次メッシュ内には100m細分メッシュのポリゴンが約63万件含まれる。素朴に全件を
  DBへ格納せず、投影座標系で重心を計算してからMeshCellへ空間結合し、メッシュごとの
  最頻値（mode）を代表の土地利用種別として保存する。

### 4.3 標高（`ingest/dem.py`）

基盤地図情報の生GML DEM（`fgd.gsi.go.jp`）は現在DNSが解決せず、後継の
`service.gsi.go.jp/kiban` は利用者登録（氏名・住所等の入力）が必要。代わりに
**認証不要の地理院タイル（標高タイル、PNG形式）** を使う。

RGB→標高値の変換式は `https://maps.gsi.go.jp/development/demtile.html` に記載が
あるが、**単純にHTMLをテキスト化すると指数表記（`<sup>`タグ）が失われ、数式の桁が
文字化けする**（`2^23` が `223` に見えるなど）。生HTMLを直接確認して以下の正しい式を
検証済み：

```
x = 2^16 R + 2^8 G + B
x <  2^23  →  h = x * u       (u = 0.01m)
x == 2^23  →  h = NA          ((R,G,B)=(128,0,0) がこれに一致)
x >  2^23  →  h = (x - 2^24) * u
```

DEM5A（航空レーザ、最高精度）→DEM5B→DEM10B（全国網羅）の順にフォールバックする
（タイル単位で404なら次を試す）。ズームレベルは5m解像度に対応する15を使用。
対象エリアの標高実測値は **-6.36m〜30.13m、平均4.77m** で、江東区・江戸川区の
既知の海抜ゼロ以下エリアと整合する。

### 4.4 OpenStreetMap 道路網（`ingest/osm_roads.py`）

Overpass API（`overpass-api.de`）。**汎用的なUser-Agentだと`406 Not Acceptable`で
拒否される**ため、識別可能なUser-Agentを付与する必要がある（Overpass APIの利用
ガイドラインにも明記）。道路（`highway`タグ）を取得し、メッシュごとの重なり長を
線分とメッシュポリゴンの厳密な交差計算（`intersection().length`）で求める（中心点
判定などの近似は使っていない）。対象エリアで7,082メッシュに道路延長を付与、
平均約1,322m/メッシュ、最大約4,494m/メッシュ（都市部の道路密度として妥当）。

### 4.5 国土交通省 道路陥没オープンデータ（`ingest/kanbotsu_xlsx.py`）

令和5・6年度分がXLSXで直接ダウンロード可能（認証不要）。**シート構成・列位置が
年度によって微妙に異なる**ことを実際にファイルを開いて確認した（例：R6の
「都道府県」シートには R5に無い「管理者区分」列が追加されており、この列は
中身が `#REF!`（Excelの参照エラー）で実質使用不可）。このため列位置を固定せず、
ヘッダー行のラベル文字列から動的に検出する実装にしてある（`_find_column_positions`）。

地名文字列（「〇〇市〇〇地先」形式）は国土地理院の地名検索API
（`msearch.gsi.go.jp/address-search/AddressSearch`）でジオコーディングする。
字・大字レベルの精度（番地は解決しない）。東京都・埼玉県の対象934件のうち
**933件（99.9%）をジオコーディング成功**。要因施設の内訳は「下水道」が
**341件（36.5%）で最多**――本プロジェクトの前提（下水道管路の老朽化が道路陥没の
主要因）を実データが直接裏付けている。

---

## 5. 誠実性フレームワーク

README の表を参照。本書ではモデル単位で対応関係を明記する：

- **実データ**：`DisplacementVelocity`、`GroundClass.land_use_class`/`elevation_m`、
  `RoadExposure`、`SubsidenceEvent` はすべて上記5データソースからの実測・実取得値。
- **合成データ**：疑似管路（`triage.SyntheticPipe`、実装済み）。OSM道路センターライン上へ
  合成配置し、布設年・管種はseed固定の架空値。優先度算出のヒューリスティック手法自体も
  未検証であり、`Illustrative`バッジで実データと厳密に区別する（詳細は第9章）。
- **InSARラスタの来歴（`DisplacementAcquisition`）**：GSIの変位速度APIには年度を選択する
  手段が無いため（4.1章）、取込のたびに取得した GeoTIFF の SHA-256（`content_sha256`、
  一意制約）と取得時刻（`retrieved_at`）を監査記録として残す。`fiscal_year`は
  この記録に紐づくbest-effortの表示ラベルに過ぎず、`fiscal_year_provenance`が
  `"unverified_retrieval_time"`の場合は「取得時点の年度を推定しただけで、実際の成果年度
  との一致は未検証」であることを常に意味する。ラベルの検証可否をユーザーへ隠さない
  （`api/views.py`の`fiscal_year_provenance`、`AnalysisRun.metrics_json.displacement_provenance`）。
  かつてこの区別が無く固定文字列を断定的に刻んでいたことが、来歴汚染バグ（F9）として
  敵対的監査で指摘され、本節の設計に修正された。
- **未実装（原理的制約ではなくスコープ判断）**：`PrecipitationObservation`（第6章）、
  `GroundClass.geology_class`/`ground_classification`（第2章）。

### 5.1 推論レベルの誠実性（第7章の確定判断を受けて）

上記はデータの「来歴」に関する誠実性である。第7章で確定した通り、Monitor解析レイヤー
（`backend/analysis/`）はさらに「推論の主張」レベルの誠実性を要求される。実装・審査の
双方で常に意識すべき既知リスクを、モデル単位で明記する：

- **標高の循環（`GroundClass.elevation_m`）**：本AOI（江東区・江戸川区）は「ゼロメートル
  地帯」であり、現在の低い標高は過去の地盤沈下の累積結果である。標高を「沈下を説明する
  独立変数」として扱う分析（例：残差回帰）は、沈下を沈下自身で説明する循環に陥る。この
  ため`backend/analysis/`では標高を回帰の説明変数として使わない。
- **地質データ未投入（`GroundClass.geology_class`）**：「地質で説明できない異常」という
  言い回しは、地質が実際にモデルへ入っていない限り虚偽ラベルになる。本プロジェクトは
  地質を分析に一切使わないため、この種の主張自体を行わない。
- **InSARのノイズフロア（`DisplacementVelocity.velocity_cm_per_year`）**：本AOI平均は
  -0.072cm/年（-0.72mm/年）で、GSI公表精度（概ね±数mm/年）の限界に近い。地図・API
  ともにセル単位の符号（沈下/隆起）を解釈しない旨を常設の注記として表示する
  （`backend/api/views.py`の`DISCLAIMERS`、フロントの`HonestyPanel`/`Legend`）。
- **字重心へのイベント集約（`SubsidenceEvent`）**：`ingest/kanbotsu_xlsx.py`のジオコー
  ディングは字・大字レベルの精度で、同一字内の複数件は同一座標にスナップされる。個々の
  イベントを精密な点として扱わず、常に「字（重心座標）単位の面」として集計・検定する
  （`backend/analysis/groundtruth.py`）。
- **n不足によるオーバーフィット**：AOI内の実イベント数は`report_aoi_events`コマンドで
  毎回実測し、下水道原因イベントが`PERMUTATION_TEST_MIN_N`（既定10）未満なら、いかなる
  統計的主張も行わず記述に留める。多変量回帰・スコアリングモデルの学習は本プロジェクトの
  どこにも実装しない（第7章）。

### 5.2 誠実性の強制と再現（CI・再現ハーネス）

上記のガードレール群 ── 生スコア（`priority_index`）の非公開、tier並び順の
`priority_index`非依存化（9.4章）、免責文の単一情報源化（9.2章）、来歴ラベルの
検証可否表示、反循環ガード（第7章）など、独立敵対的監査（PR #6〜#9）で追加・強化された
不変条件 ── は、`backend/{core,ingest,analysis,api,triage}/tests.py`の約40テストとして
実装されている。これらは **GitHub Actions CI（`.github/workflows/ci.yml`）が全PR・全pushで
実行**し、いずれかが回帰すればマージがブロックされる。「テストで誠実性を守る」という主張を、
手動実行に頼らず強制力のある不変条件にするための仕組みである。

加えて、§7.3・§9.3の実測値（片側p=0.9616、Moran's I=0.797、道路長とのSpearman順位相関
ρ=0.903）は、文書に記載するだけでは第三者が検証できない（生データは`.gitignore`され、
ライブソースはdriftする）。そこで、これらの数値の直接の裏付けとなった入力データ一式を
来歴付きゴールデンスナップショット（`backend/analysis/fixtures/golden_snapshot.json.gz`、
来歴は`docs/SNAPSHOT.md`）としてコミットし、`./scripts/reproduce.sh`がクリーンな使い捨て
環境でこれを読み込み、同一seed（=42）で`build_monitor`→`generate_triage`を再実行して
期待値と照合する。CIの`reproduce`ジョブがこれを毎回実行し、driftを継続検知する。数値の
単一情報源は`backend/analysis/fixtures/golden_snapshot_expected_metrics.json`であり、
本書・README・`docs/ASSESSMENT.md`の数値はそこからの転記に過ぎない（p値を固定文字列で
埋め込み検定の再実行に追随できなかったF9/独立敵対的監査の教訓を、文書層にも適用したもの）。

なお、同じゴールデンスナップショットは**オフラインのデモ投入**にも再利用される。
`docker compose up`後のDBは空で、実データ表示には本来`ingest_all`→`build_monitor`→
`generate_triage`の手動実行と政府エンドポイントへの実HTTP取得（遅い・ネット必須）が要る。
`seed_demo`管理コマンド（`make demo`が呼ぶ）は、reproduceと同じ`loaddata`→`build_monitor`
→`generate_triage`連鎖を「検証」ではなく「セットアップ」として実行し、外部に一切触れず
実データ相当の画面を出せる状態を作る（reproduce_assessmentと違い、既投入なら冪等にskip、
数値照合はしない）。**デモに出る数値は`reproduce`ジョブが継続検証する値と同一であり、
"見栄え用の別データ"を作らないことで、開示済みの所見からdriftできないようにしている。**

---

## 6. スコープ外とした判断

README の Future Work と併せて、判断の経緯を記録する。

**降水量データ**：計画段階では「解析雨量1kmメッシュ」を想定していたが、実際に
気象庁・国交省・JMBSC等のアクセス方法を調査した結果、**無償・自動・historical・
グリッド解像度の4条件を同時に満たす公式ルートが存在しない**と判明した：
DIAS/XRAINは長期メンテナンス中で復旧時期未定、気象庁の`obsdl`は自動化を明示的に
非推奨、無償の代替（`river.go.jp`）は1990年代フレームセット構成のサイトで
安定したスクレイピングが困難。当初のプラン段階で降水量は既に「susceptibility特徴量
にしない、局所陥没の引き金文脈のみ」に格下げ済みだった（circular/overclaim回避の
監査対応）ため、無理にスクレイパーを実装せず本PRのスコープから外した。

**ALOS-4自前SLC処理・自前InSAR時系列・クラウドデプロイ**：README参照。

**自前InSAR処理をスコープ外とした判断の事後検証**：この判断は「自前処理の技術的難度」を
理由にしたものではなく、**単一ペアの感度が要件に届かない**ことを理由にしている。
その前提が正しいかを、別リポジトリ `s1-insar-minimal` で Sentinel-1 IW SLC の
単一サブスワス・単一バーストペアを ESA SNAP (gpt) で実際に処理して確かめた。

実験設計は、同一の相対軌道（path 46 降交・Sentinel-1A）・同一バースト（`046_097520_IW2`）の
4シーンから3ペア（冬12日／春12日／132日横断）。垂直ベースラインが全ペアで臨界ベースライン
（7,242 m）の 1% 未満なので、幾何デコリレーションを排除して時間デコリレーションだけを見られる。

得られた数値と、それが本プロジェクトの判断に対して持つ意味：

| 実測値 | 本プロジェクトへの含意 |
|---|---|
| 密集市街地の12日ペアで γ̂ = 0.81 | **コヒーレンスは律速要因ではない。** 位相は十分読める |
| 残差位相 0.41 mm（1 km 内）／≥1.99 mm（シーン全体、視線方向） | 律速要因はこちら。大気＋軌道＋DEM＋実変位の合計で、単一ペアでは分離できない |
| 沈下 10 mm/年 の12日ペア信号 = 0.254 mm（視線方向） | 最良ペアの短波長残差の 0.62 倍。**1 km スケールのばらつきに埋もれる** |
| 12日回帰で3年積んだ場合の速度精度 ±0.6〜1.2 mm/年 | mm/年級には複数年の時系列解析が要る |

副次的に、コヒーレンス推定量 γ̂ の上方バイアスをモンテカルロで定量し（独立サンプル数
N_eff ≈ 29 で、真に無相関でも E[γ̂] = 0.165、95パーセンタイル 0.319）、東京湾の水域で
実測した γ̂ = 0.19〜0.24 と突き合わせて推定器の評価が観測と整合することを確認した。
第5章の誠実性フレームワークと同じ姿勢で、**設計時の予想（季節による γ 低下・132日ペアの
崩壊）が外れたことも、その理由（植生域は12日で既にノイズフロアに達しており崩れる余地が
なかった／市街地は季節不感）とともにそのまま開示している。**

この検証は公開成果（Lバンド）の検証**ではない**。センサーも波長も期間も異なるため、
Cバンド単一ペアでLバンド時系列成果を検証することは原理的にできない。
確かめたのは「単一ペアでは要件に届かない」という側の主張だけである。

---

## 7. Monitor解析レイヤーの設計判断（確定）

**Monitor解析エンジンの「加速度・トレンド変化検出」設計**。当初計画（時系列の2階差で
沈下の加速を検出）は、第4.1章の制約（GSIから得られるのは年度スナップショット1枚のみ）
により、稠密な時系列データを前提にした形では実装できないことが判明した。この制約を
受け、当初提示していた3つの選択肢を検討した：

1. **残差異常検出**：地質・標高・土地利用から「期待される沈下速度」を回帰し、実測速度
   との残差が大きいメッシュを「地質だけでは説明できない異常」として検出する案。
2. **点単位の時系列APIをさらに調査**：「時系列グラフ閲覧」機能の実際のAPIを調べ、
   メッシュ単位で稠密な時系列が取れないか確認する案。
3. **陥没密度回帰**：InSAR変位・地質・標高等を特徴量とし、道路露出をオフセットとした
   陥没報告密度の回帰分析を行う案。

### 7.1 却下の理由（独立した敵対的レビュー2回を経て）

- **Option 2（時系列API再調査）を却下**：第4.1章で「エリア一括の多時点時系列ダウンロード
  に対応するAPIは見当たらない」と実機検証済みであり、この選択肢の前提はすでに反証されて
  いる。選択肢として残すこと自体が誠実でない。
- **Option 1（残差異常検出）を却下**：`GroundClass.geology_class`が未投入のため、「地質
  で説明できない異常」という言い回しはモデルが統制していない変数についての虚偽ラベルに
  なる。加えて、標高を説明変数に使うと第5.1章で述べた循環（本AOIの低標高は過去の沈下の
  累積結果）に陥る。回帰のR²が低ければ残差はほぼ生の変位速度と同義になり、統計的加工の
  外見をまとっただけの生データ地図に帰着する。
- **Option 3（陥没密度回帰）も「回帰・スコアリングモデル」としては採用しない**：唯一、
  実在する外部イベント集合（MLIT陥没データ）に対して反証可能な検証ができる案ではあるが、
  AOI内の下水道原因イベント数は限られ（後述）、per-cellの連続的な「期待陥没率」を
  choropleth表示すると、nゲートで多変量係数が識別不能になった場合に実質「道路長の地図」
  をハザード色で偽装するだけになりかねない。これは面的トリアージ（README参照）の趣旨を
  超えた資産単位リスクの含意になり、誠実性フレームワーク（第5章）に反する。

### 7.2 確定した設計：記述統合＋グラウンドトゥルース重ね合わせ＋単一の付録検定

回帰・スコアリングモデルの学習は本プロジェクトのどこにも実装しない。代わりに
`backend/analysis/`は以下のみを行う：

1. **実データの面的統合**（`features.py`）：`MeshCell`ごとに変位速度・土地利用・標高・
   道路延長・観測イベント数を集約し`MeshSummary`として永続化する。予測・スコアは含まない。
2. **陥没イベントのグラウンドトゥルース重ね合わせ**（`groundtruth.py`、フロント
   `MonitorMap.tsx`）：実陥没を点overlayとして地図に重ね、共起を視覚的に検証可能にする。
   イベントは常に「字（重心座標）単位の面」として扱う（第5.1章）。
3. **AOI内実イベント数の正直な実測と開示**（`report_aoi_events`コマンド、
   `groundtruth.count_aoi_events`）：モデル化・検定の可否を判断する前に、まずnを測る。
4. **nゲートを通過した場合のみ、単一の空間パーミュテーション検定を付録として実施**
   （`permutation.py`）：「陥没(下水道原因)を含むセルは、含まないセルより変位速度が
   負に偏るか」を、道路曝露のある適格セルの母集団からのランダム再配置による正確検定で
   評価する。イベントは検定のターゲットとしてのみ使われ、モデルの入力特徴量には一切
   使わない（反循環）。`PERMUTATION_TEST_MIN_N`（既定10）未満なら検定自体を実施せず、
   その旨と理由を`AnalysisRun.metrics_json`に記録して正直に開示する。

#### 7.2.1 空間自己相関への対応（独立敵対的監査を経て確定）

上記の一様再配置は各適格セルを交換可能（i.i.d.）とみなすが、InSAR変位速度は空間的に
滑らか（隣接セル間で似た値を取る）であり、この前提は厳密には成り立たない。自己相関が
あると独立な標本数は見かけのkより少なく、一様nullは分散が過小になり、**p値は反保守的
（実際より小さく）出る**。

この問題への対応方針は、独立した敵対的レビューを経て以下のように確定した：

- **一様検定自体は変更しない**（`permutation.py`の該当コード・p値は不変）。空間構造を
  保存した独自の補正null（トーラス並進等）は**採用しない**：本AOIのように小さく穴の
  多い母集団では、そうした補正が「観測配置がk個すべて適格セルに乗る」有効な置き方を
  内部の密な領域に偏らせ、null分散をかえって狭めるという未定量のバイアスを持ち込みうる
  ため。加えて、本検定の観測結果は既に非有意かつ仮説と逆方向であり、いかなる空間補正も
  この結論（仮説不支持）を覆すことはできない。新たな点推定pを作ることは、追加の検証
  されないバイアスを持ち込む「厳密さの見た目」になりかねないと判断した。
- 代わりに、`morans_i_velocity`（適格プールの変位速度に対する大域Moran's I、ルーク
  隣接）を**記述的診断**として算出し、一様p値と併記する。有意性検定やnull補正には
  用いない。
- 一様p値には「空間自己相関を無視した反保守的な下限値であり、真のp値はこれ以上」と
  いう解釈文（`p_value_interpretation`）を機械的に付与し、`build_monitor`コマンド出力・
  APIレスポンス・フロントのHonestyPanelの全てで一貫して開示する。

### 7.3 実測結果（2025年度データ、AnalysisRun #3実行）

`report_aoi_events`の実測：AOI内 陥没イベント総数 **129件**、うち下水道原因
**30件**（占有字数25）。これは`PERMUTATION_TEST_MIN_N=10`を上回るため、付録検定を
実施した。結果：観測差 **+0.0216 cm/年**（陥没セルの平均変位速度は非陥没セルよりわずかに
正寄り＝隆起側。仮説と逆方向）、片側p値 **0.962**、両側p値 **0.122**（N=10,000回の
パーミュテーション、seed=42、k=25）。

**空間自己相関診断**：適格プールの変位速度に対する大域Moran's I（ルーク隣接）は
**0.797**（自己相関なしの期待値 −0.0001 を大きく上回る）。これは隣接セル間の変位速度が
強く類似していること＝一様検定が仮定する「交換可能性」が実際には成り立っていないことを
裏付ける。したがって上記の片側p=0.962・両側p=0.122は**空間自己相関を無視した反保守的な
下限値**であり、真のp値はこれ以上と考えられる。ただし観測差の符号自体が仮説と逆向き
（陥没セルはむしろ隆起側）であるため、この限界を踏まえてもなお**結論（仮説不支持）は
変わらない**。

**この結果は仮説を支持しない**。すなわち、本AOI・本データにおいて「陥没箇所は変位速度が
より負に偏る」という主張を統計的に裏付けることはできなかった。誠実性フレームワークの
原則どおり、この否定的な結果をそのまま公表する（README・HonestyPanel参照）。これは失敗
ではなく、検定機構が意図通りに機能し、都合の良い結果を選ばずに正直な結論を出したことの
証左である。

---

## 8. 参照

README.md の「参考文献・出典」を参照。本書に記載した実装固有の出典（`layers_for_sar.txt`、
`demtile.html`、KSJ datalist ページ等）はコード中のdocstringに直接記載している。

---

## 9. Triage（意思決定支援層）の詳細設計

Monitor（第2〜7章）は100%実データで完結する。Triage は、その実成果を自治体の
点検優先度判断に翻訳する意思決定支援層であり、`backend/triage/` に実装する。
独立敵対的レビューを経て、以下の設計とした。

### 9.1 モデル(`backend/triage/models.py`)

| モデル | 主な役割 |
|---|---|
| `SyntheticPipe` | 疑似管路(Illustrative)。geom(LineString)はOSM道路センターラインをそのまま流用し、道路網上への合成配置とする。布設年・管種はseed固定の疑似乱数による架空値 |
| `TriageRun` | `generate_triage`コマンド1回の実行記録(analysis.AnalysisRunの姉妹モデル)。重み・パイプ数・自己点検の相関・手法未検証の明示をmetrics_jsonに記録 |
| `MeshPriority` | メッシュ単位の優先度(analysis.MeshSummaryの姉妹モデル)。内部計算値`priority_index`と、公開用の`tier`(高/中/低)、構成実測値の分解を保持 |

### 9.2 優先度算出の誠実性設計(`backend/triage/scoring.py`)

回帰・分類モデルの学習は一切行わない。事前に固定した重み(`DEFAULT_WEIGHTS`:
沈下シグナル0.4・道路曝露0.3・過去事案0.3)による線形結合で`priority_index`を算出し、
分位ベースで`tier`(高/中/低)に離散化する。

この設計は、独立敵対的監査の以下の指摘を受けて確定した:

- **手法自体の免責(guardrail a)**：`Illustrative`バッジは、疑似管路という「データ」
  だけでなく、優先度算出という「手法」自体が未検証であることも明示しなければならない。
  本AOI・本データの空間パーミュテーション検定(第7章)は、直近の実測(§7.3)では
  「陥没(下水道原因)箇所は変位速度がより負に偏る」という仮説を**支持しなかった**。
  この検定は`build_monitor`の再実行で結果が変わり得るため、具体的なp値を
  `triage/scoring.py`や`api/views.py`に固定文字列として書かない設計とした
  (以前は「片側p=0.962」を2箇所に個別ハードコードしており、Monitor側の検定が
  再実行されても追随せず、HonestyPanelとTriagePanelで異なる数値が同時表示され
  うるという指摘を独立敵対的監査で受けた)。`analysis/permutation.py::summarize_result()`
  を単一の情報源とし、最新の`AnalysisRun.metrics_json`からその場で要約した文言を、
  `TriageRun.metrics_json.method_validation_note`(生成時点のスナップショット)と
  API `disclaimers`(リクエスト時点のライブ値)の両方に用いる。沈下シグナル項の重みは
  検証済みの予測係数ではなく事前の業務ヒューリスティックに過ぎないことを、
  `TriageRun.metrics_json.method_validated`(常にFalse)・`method_validation_note`・
  APIの`disclaimers`・フロントの常設バッジ・パネルの4箇所で一貫して明示する。
- **符号解釈の一貫しない扱いの自認**：Monitor側の免責(`api/views.py DISCLAIMERS`)は
  GSI変位速度の精度限界(概ね±数mm/年)を理由に「セル単位の符号(沈下/隆起)は
  解釈しない」と明言する。一方でTriageの`subsidence_signal`
  (`triage/scoring.py::compute_priority_index`)は、まさにそのセル符号を
  `clip(upper=0).abs()`で沈下方向のみ抽出し、重み0.4で優先度算出に用いる。
  これは精度限界下で意味を持たないと自ら述べた値を、意思決定支援の入力としては
  敢えて使うという一貫しない選択であり、隠すのではなく`_triage_disclaimers()`
  (`api/views.py`)で明示的に自認する。手法が未検証(`method_validated=False`)
  である理由の一つでもある。
- **連続スコアを公開しない(guardrail b)**：`priority_index`はAPIでは一切返さない。
  `/api/triage/pipes/`・`/api/triage/ranking/`は`tier`のみを公開し、構成要素
  (velocity_cm_per_year・road_length_m・sewer_event_count等)は実測値のまま分解表示する。
- **合成入力への視覚的束縛(guardrail c)**：フロントの疑似管路レイヤーは破線・紫系の
  専用配色(`tierColor`)で描画し、Monitorの実データレイヤー(道路延長の実線・青系配色)
  とは明確に区別する。
- **バッジを剥がすエクスポートを作らない(guardrail d)**：本実装にPDF/CSVエクスポート
  機能は存在しない。将来追加する場合も、`Illustrative`バッジと免責文を含まない出力は
  提供しないこと。
- **架空属性の明示(guardrail e)**：`pipe_material`の選択肢はすべて「(例示)」を付した
  ラベルとし(`PIPE_MATERIAL_CHOICES`)、実在の管路分類体系と誤認されないようにする。

### 9.3 自己点検：道路長ベースラインとのSpearman順位相関(実測結果)

docs/SPEC.md §7.1は、per-cell連続の「期待陥没率」choroplethについて「実質『道路長の
地図』をハザード色で偽装するだけになりかねない」という懸念から却下した。Triageの
優先度ヒューリスティックが同じ問題を抱えていないかを自己点検するため、
`priority_index`と`road_length_m`単独ランキングとのSpearman順位相関
(`baseline_road_length_spearman_rho`)を毎回算出し、`TriageRun.metrics_json`と
APIレスポンスの両方に必ず記録する(`triage/scoring.py::baseline_correlation`)。

**実測結果(本AOI、デフォルト重み、seed=42)**：ρ = **0.903**。

この値は高く、現状のデフォルト重みでは、優先度ランキングが「道路長だけの単純な
ランキング」にかなり近いことを意味する。原因として、適格プール(road_length_m>0)内で
`past_events`(下水道原因の陥没報告数)自体が道路曝露と機構的に相関しやすいこと
(道路が長い/交通量が多いメッシュほど陥没が報告される機会も多い。第2章
`RoadExposure`のdocstring参照)が挙げられる。この結果は仮説通りの成功を示すものでは
なく、Monitorの否定的な検定結果(第7章)と同じ精神で、そのまま正直に公表する。
将来、重み付けの見直しや実管路特徴量の追加によってこの相関を下げられるかは
未検証の課題として残す(商用化ロードマップ「予測価値の実証」参照)。

### 9.4 API(`backend/api/views.py`)

- `GET /api/triage/pipes/`：疑似管路のLineString GeoJSON。各featureの`properties`に
  `illustrative: true`・`method_validated: false`・`tier`・架空の`install_year`/
  `pipe_material`を含む。連続スコアは含まない。
- `GET /api/triage/ranking/`：メッシュ単位の優先度ランキング。`tier`と構成実測値の
  分解、`metrics`(`method_validated`・`method_validation_note`・
  `baseline_road_length_spearman_rho`・パイプ数)、Triage専用`disclaimers`を返す。
  `priority_index`はレスポンスに一切含まれない。並び順は`tier`(高→中→低)、tier内は
  `mesh_code`昇順であり、`priority_index`には一切依存しない
  (`api/views.py::TIER_RANKING_ORDER`)。tierだけで降順ソートすると、tier内の並びが
  `priority_index`降順とほぼ一致し、レスポンスの並び順から連続スコアの序数を
  ほぼ復元できてしまう――「連続スコアはtierのみに縮約して非公開にする」
  (guardrail b)という趣旨が並び順から骨抜きになるという独立敵対的監査の指摘を
  受けて、tier内の順序をスコアと無関係な`mesh_code`に固定した。

### 9.5 コマンド(`generate_triage`)

`build_monitor`と同型のオーケストレーション(`backend/triage/build.py`)。疑似管路は
Overpass APIへの再アクセスを避けるため、既存分があれば`--regenerate-pipes`を
指定しない限り再取得しない(冪等)。同一seedなら疑似管路の生成・優先度算出とも
完全に再現する(`triage/tests.py`で決定性を検証)。

---

## 10. フロントエンドUIの設計判断（Monitor コックピットと初見向け配慮）

フロントは React + TypeScript + Vite（`frontend/`）、地図は deck.gl + MapLibre GL、
下図タイルは国土地理院 淡色地図（認証不要・来歴がデータソースと一貫）を用いる。API
クライアント（`frontend/src/api/client.ts`）は相対 `/api` を叩くだけで、連続スコア
（`priority_index`）を型にも持たない（9.4章のguardrail bをフロント型でも担保）。

本章は、UIが第5章・第9章の誠実性の不変条件を「画面上でも」崩さないための設計判断を
記録する。UIの追加はあくまで**実データの読み取り性と可読性**であり、新たな予測・
スコア・順位を露出しない。

### 10.1 Monitor 選択コックピット

当初のMonitor UIはホバー時のツールチップのみで、マウスを外すと情報が消え、落ち着いて
読めず比較もできなかった。これを、メッシュのクリックで**右レールに常設の詳細**を出す
「状況把握コックピット」に拡張した（`frontend/src/components/MeshDetailPanel.tsx`、
`App.tsx`、`map/MonitorMap.tsx`）。

- **選択状態は `App.tsx` を単一の情報源とする**（`selectedMesh`）。地図クリックは
  `MonitorMap` から `onSelectMesh` で通知し、右レールは詳細パネル＋既存
  `HonestyPanel` を縦積みして表示する。地図は一切覆わない。
- **表示項目は実データのみ**：メッシュコード・変位速度（実測）・土地利用・標高・
  道路延長・過去の陥没報告件数。過去陥没はメッシュ属性（`event_count` /
  `sewer_event_count`）をそのまま用い、空間結合で新たな主張を作らない。詳細パネル下部に
  「これは状況把握であり、陥没予測ではありません」を常設（免責は§5の壁の免責への
  **追加**であって置換ではない）。
- **空状態は「未選択」であって「安全」ではない**。未選択時は中立色で「セルをクリック
  すると実測値を表示（未選択は"安全"を意味しません）」と明示する。
- **Monitor の選択ハイライトは中立色（青）のソリッド枠**。発散配色や「赤＝危険」は
  使わない（§5.1のノイズフロア注記と同じ精神で、色でハザードを示唆しない）。選択セル
  1件だけを別レイヤーで縁取りし、全メッシュのfill再評価を避ける。なお Triage の行連動
  ハイライト（§10.4）は同じ「選択」でも意味・文脈が異なる（実データではなく Illustrative
  なワークリストの位置指示）ため、**見た目を意図的に変え**、中立灰の破線ロケータにする。
- **選択解除**は「同一セルの再クリック（トグル）」とパネルの「解除」ボタンで担保する。
  deck.gl のオーバーレイ構成では地図の背景（何もない場所）クリックで `onClick` が
  発火しないため、背景クリックに依存しない確実な導線とした。

### 10.2 初見向けの配慮（ポートフォリオとしての可読性）

自治体職員など初見の読み手が「何をするツールか／まず何をすればよいか／専門用語」で
つまずかないよう、誠実性フレーミング（予測ではなく状況把握）を崩さない範囲で3点を
加えた。

- **この地図の見方**（`components/MapGuide.tsx`）：右レール上部の折りたたみ式イントロ。
  「無償の衛星データ（InSAR）で見た地盤の動きを、過去の道路陥没と重ねて眺める地図。
  将来の陥没を予測するものではない」という要旨と3ステップの使い方を日常語で示す。
- **クリック誘導コーチマーク**（`map/MonitorMap.tsx`）：初回のみ「区画をクリック→右で
  実測値」を地図上に表示し、最初の選択で消える。`prefers-reduced-motion` を尊重する。
- **用語ツールチップ**（`components/Term.tsx`）：ⓘマーカー＋ホバー/フォーカスで平易な
  補足を出す軽量コンポーネント（`aria-label` で読み上げにも対応）。値そのものは書き
  換えない。

### 10.3 専門用語の平易化（表示ラベル・補足のみ／数値は不変）

第5章・第7章・第9章の統計用語は、意味を薄めずに表示ラベルと補足だけを平易化した。
**数値・仮説の文言・バックエンド由来の但し書き（片側＝自己相関を無視した下限値、
Moran's I 診断など）は不変**であり、単一情報源（§5.2）からの転記である点も変わらない。

| 元の表記 | 画面表示 | 補足（ツールチップ等） |
|---|---|---|
| AOI | 対象エリア | 対象範囲（デモは江東区・江戸川区・八潮市周辺）。AOI＝Area of Interest |
| p値 | p値（ⓘ） | 「関係が無くても偶然でこれくらいの差が出る確率」の目安。0.05未満で偶然とは考えにくい |
| 空間パーミュテーション検定 | 同（ⓘ） | ラベルを何千通りも並べ替えて、実際の差が偶然の範囲かを比べる方法 |
| 片側／両側 | 同（ⓘ） | 一方向だけを見た／向きを問わず差を見た p値 |
| n（検定対象セル数） | n（検定に使えたセル数）（ⓘ） | 少ないほど結果は不安定。`underpowered` → 「サンプルが少なく不確実」 |
| 空間自己相関診断（Moran's I） | ご近所効果の診断（Moran's I ⓘ） | 近くのセル同士で値が似ている度合い。大きいほどp値が甘めに出る |
| Spearman順位相関 ρ | 順位の一致度（Spearman ρ）（ⓘ） | 2つの並び順の一致度（-1〜1）。1に近いほどほぼ同じ並び |
| tier順 | 優先度ランク（tier）順（ⓘ） | 大まかな段階（高/中/低）。細かい順位はあえて出さない（9.4章 guardrail b） |

平易化は「予測しているかのような言い換え」を避けることを最優先とし、イントロや補足は
いずれも「予測ではなく状況把握」の枠を先頭に置く。tier のみの公開（連続スコア非露出）と
いう9.4章の設計は、tooltip の文言でも「細かい順位はあえて出さない」と明示して補強する。

### 10.4 Triage 行→地図連動の誠実性設計

Triage ランキング（`components/TriagePanel.tsx`）は静的なリストで、行の `mesh_code` が
地図上のどこかと結びつかなかった。行を選ぶと地図がその区画へ飛んで位置を示す連携を
加えた（`App.tsx` の `triageFocusCode`、`map/MonitorMap.tsx` の flyTo と `mesh-triage-locator`
レイヤー）。ただしこの連携は *実データのメッシュ幾何を Illustrative な文脈で選択対象にする*
新しい混同リスクを持つため、次の判断で誠実性を担保する。

- **一方向のみ**。連携はランキング→地図の片方向で、Triage での地図クリックによるメッシュ
  選択は引き続き無効（`pickable:!isTriage`）、実データ配色の淡色化も維持する。実データと
  合成データ（疑似管路）の視覚的主従関係を崩さない。
- **ハイライトは実測ハザードの再着色ではなく「ロケータ」**。Monitor のソリッド青枠を
  流用せず、本コードベース確立の「破線＝Illustrative」文法（疑似管路レイヤーと同じ）に
  合わせ、中立灰の破線枠にする。tier 別の色や発散配色は使わない。行が指す区画を示すだけで、
  そのセルが「危険と判定された」という含意を作らない。
- **flyTo は上限ズームで寄せすぎない**。陥没事案は字・大字レベルのジオコーディング
  （同一字＝同一座標）で位置精度が粗い（§4.5）。番地レベルまで寄ると根拠のない空間精度を
  演出するため、ズームは上限付き（メッシュが収まる程度）にとどめる。
- **連番を優先キューに昇格させない**。ランキングは `<ol>` ではなく `<ul>` で描画し、優先度
  シグナルは tier バッジのみに限定する（順位番号やカウンタは付けない）。tier 内順序が
  `priority_index` ではなく `mesh_code` 昇順であるという §9.4 の設計と体験レベルで一貫させる。
- **連動できない行は無反応にしない**。`mesh/summary`（AnalysisRun）と `triage/ranking`
  （TriageRun）は別 run のため、行の `mesh_code` が地図に存在する保証がない。地図に無い行は
  クリック不可にし「地図非対応」と明示して、押せるのに何も起きない挙動を作らない。

### 10.5 Monitor 地名検索の設計判断（D4）

メッシュコード（例 `250m-19--120`）は人間には不透明で、地図クリックでしか目的の区画へ
たどり着けなかった。地名・住所で入力して該当区画へ flyTo し選択する検索を Monitor に加えた
（`map/MonitorMap.tsx` の検索フォーム＋`api/client.ts` の `fetchGeocode`）。名称→座標の
ジオコーディングという性質上、来歴・精度・被覆で誠実性の判断を要する。

- **来歴の一貫性**。ジオコーダは国土地理院 地名検索 API を用いる。これは陥没イベント座標の
  ジオコード（`ingest/kanbotsu_xlsx.py`）や地図タイルと**同じ来歴（国土地理院）**であり、
  ブラウザから直接呼ぶ（CORS 許可済み）。新たな第三者依存を持ち込まない。
- **AOI 外へ誘導しない**。ジオコーダは全国の候補を返すため、`core/aoi.py` の `DEMO_AOI_BBOX`
  と同じ範囲内の候補だけを採用する。範囲外しか無ければ飛ばさず「対象エリア外」と明示する
  （データ被覆の外を、あたかも対象であるかのように見せない）。
- **粗い位置精度を過大演出しない**。ジオコーディングは字・大字レベルで粗い（§4.5）。flyTo は
  §10.4 と同じ上限ズームにとどめ、結果は「おおよその位置（字/大字レベル）」と明示する。
- **予測ではなく実測の表示**。検索座標を含む 250m メッシュを選択し、右レールには実測値のみを
  出す（§10.1 と同じ詳細パネル）。「その地点が陥没する」等の予測は一切示さない。区画は
  フロントの point-in-polygon で求める（バックエンドの格子割当と実質同じ結果）。

## 11. 単一URL配信・シェア用静的サイトの設計判断（D7）

これまで閲覧には Django API（`:8000`）と Vite dev server（`:5173`）の2プロセスが必要で、
開発者本人のPC上でしか動かなかった。「リンク1つで共有できる」形にするため、**API応答を
静的JSONへ焼き固め、サーバ・DB不要の完全静的サイトとして GitHub Pages に配信する**方式を採った。

- **なぜ静的で成立するか**。閲覧デモのデータは golden snapshot で固定（§5.2）であり、フロントが
  呼ぶのは `mesh/summary` / `events` / `analysis/run/latest` / `triage/pipes` / `triage/ranking` の
  **パラメータなし5エンドポイントのみ**（`fetchEvents` は絞り込み引数なしで呼ばれ、
  下水道原因の色分けは `map/MonitorMap.tsx` 側で行う）。よって5ファイルの焼き固めで足りる。
  地図タイル・地名検索は国土地理院の公開API（CORS許可済み）を直接呼ぶため静的サイトでも動く。
- **ロジックを二重化しない**。`api/management/commands/export_static.py` は独自シリアライズを
  書かず、`RequestFactory` で**本物の view 関数を呼び `response.content` をそのまま書き出す**。
  したがって焼き固めJSONはライブAPIと**バイト等価**であり、"見栄え用の別データ" を作らない
  （誠実性フレームワーク §5 と同じ思想。golden snapshot 再現ハーネスの延長）。
- **ズレを機械で強制する**。`AnalysisRun.created_at` は `auto_now_add` で、`seed_demo` は
  `build_monitor`/`generate_triage` を毎回新規実行して新しい pk・時刻の run を作るため、
  `created_at` / `run_id` / `run.id` は seed のたびに変わる。これらは検証対象データではなく
  来歴ノイズなので、`export_static --check` は **その allowlist だけを除外した意味的比較**で
  コミット済みJSONと再生成結果を照合する（substance は seed=42＋固定snapshot で決定的）。
  CI の独立ジョブ `static-export` がこれを全PR・全pushで走らせ、分析ロジックやsnapshotを
  変えたのに `make static` で焼き直し忘れると落ちる。時刻/pkだけの差では落とさない。
- **配信パスへの追従**。フロントの `api/client.ts` は静的モード（`VITE_DATA_SOURCE=static`、
  `npm run build:static`）で `import.meta.env.BASE_URL` 起点に焼き固めJSONを読む。Pages の
  プロジェクトサイト用サブパス（`/sinkscope/`）を **絶対 base** にすることで assets と MapLibre
  ワーカーのURL解決が安定する（相対 `./` はワーカー読込が壊れうる）。通常の `npm run dev` /
  `build` は従来どおりライブAPI（`/api/`）を叩き、開発フローは不変。
- **主張の範囲**。共有するのは **固定デモのスナップショット** であって、認証・自動更新・SLAを
  伴うライブサービスではない（その本番運用は商用化ロードマップのスコープ）。
- **稼働状況**。GitHub Pages（Source = GitHub Actions）で公開済み。
  公開URL: https://ops324.github.io/sinkscope/ 。実配信で確認したのは、Monitor の
  国土地理院タイル＋250mメッシュ＋陥没事案の描画、Triage の疑似管路 51,084 本の描画、
  および LFS 管理下の `triage-pipes.json`（約22MB）が実体として配信されること。
  LFS はポインタのまま push しても push 自体は成功してしまうため、**別ディレクトリへ
  クローンし直して実体を取得できるか**を配信確認の必須項目とする（`CLAUDE.md` 参照）。

