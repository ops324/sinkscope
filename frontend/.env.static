# 単一URL配信(D7)の静的ビルド用の環境変数。`vite build --mode static`(npm run build:static)
# のときだけ読まれる。フロントのAPIクライアント(src/api/client.ts)がライブAPI(/api/)ではなく
# コミット済みの焼き固めJSON(public/data/*.json)を読むよう切り替える。
# .gitignore の .env.* 無視から明示的に除外してコミットしている(!frontend/.env.static)。
VITE_DATA_SOURCE=static
